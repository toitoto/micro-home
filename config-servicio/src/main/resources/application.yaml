server:
  port: 8015
spring:
  cloud:
    config:
      server:
        git:
          default-label: zipkin-sleuth
          uri: https://github.com/toitoto/micro-home
          search-paths: config-data
  application:
    name: config-servicio